#!/bin/bash

if [ "$1" != "" ]; then
    extension="${1##*.}"
    filename="${1%.*}"
    resized="${filename}_emoji.${extension}"
    echo "Emojifying $1 to $resized"
    convert "$1" -resize 128x128 "$resized"
fi
