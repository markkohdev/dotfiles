#!/usr/bin/bash

# Custom bash aliases
alias example='echo "Your alias"'

alias webserver='cd /opt/code/webserver/'
alias ngapp='cd /opt/code/webserver/'

alias optcode='sshfs maadbox:/opt/code /home/mark/dev/code'
alias maadbox='ssh maadbox'
alias optcode2='sshfs jambox:/opt/code /home/mark/dev/code'
alias jambox='ssh jambox'

alias hibernate='sudo pm-hibernate'

alias fixsound='pulseaudio -k'
